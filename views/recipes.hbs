<div class="recipes">
  {{#each recipes as | recipe |}}
    <div class="recipe">
      {{> detailedRecipe recipe=recipe }}
      <a href="/recipe/{{recipe._id}}"></a>
    </div>
  {{else}}
    <p>You should <a href="/recipes/new">add some recipes</a></p>
  {{/each}}
</div>